# Server
server.port=8080

# H2 Database (swap for MySQL/PostgreSQL in production)
spring.datasource.url=jdbc:postgresql://localhost:5432/mangavault
  #jdbc:h2:file:./data/mangavault;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.postgresql.Driver
#org.h2.Driver
spring.datasource.username=postgres
  #sa
spring.datasource.password=CONEJO.pita2
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
  #org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# File upload limits
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=500MB

# Upload directory
app.upload.dir=./uploads

# Logging
logging.level.com.mangareader=DEBUG
